# This file is intentionally minimal.
# The canonical Render Blueprint configuration lives at the repository root:
#   render.yaml
#
# Use that file when connecting your repo to Render as a Blueprint.
